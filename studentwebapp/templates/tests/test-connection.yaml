apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "studentwebapp.fullname" . }}-test-connection"
  labels:
    {{- include "studentwebapp.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "studentwebapp.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
